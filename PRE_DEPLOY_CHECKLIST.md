# ✅ Чеклист подготовки к деплою на Beget

## 1. Проверка кода

### Файлы и структура

- [x] Все файлы закоммичены в Git
- [x] `.env` в `.gitignore` (не попадет в репозиторий)
- [x] `.env.production` создан (будет использован для сборки)
- [x] `netlify.toml` создан (не нужен для Beget, но не мешает)
- [x] `.htaccess.example` создан
- [x] `deploy.sh` создан и исполняемый

### Функциональность

- [ ] Сайт работает локально (`npm run dev`)
- [ ] Нет ошибок в консоли браузера
- [ ] Все страницы открываются
- [ ] Навигация работает
- [ ] Формы отправляются (показывают успех в DEV MODE)
- [ ] Изображения отображаются
- [ ] Анимации работают

## 2. Создание .env.production

Создайте файл `.env.production` в корне проекта:

```bash
VITE_FIREBASE_API_KEY=AIzaSyCzVjhTuCdqieKzCiMT7ZAhlNzCVijz74Y
VITE_FIREBASE_AUTH_DOMAIN=repair-51654.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=repair-51654
VITE_FIREBASE_STORAGE_BUCKET=repair-51654.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=1052060993826
VITE_FIREBASE_APP_ID=1:1052060993826:web:075d35c002f4b1340dbd7b
VITE_TELEGRAM_BOT_TOKEN=7568723671:AAEIOqwkohF1cHct2_EiveI2GiWf4fzRaZY
VITE_TELEGRAM_CHAT_ID=1704115979
```

**Команда:**

```bash
cp .env .env.production
```

## 3. Сборка проекта

### Запустите сборку:

```bash
npm run build
```

### Проверьте результат:

- [x] Сборка прошла без ошибок
- [x] Папка `dist` создана
- [x] В `dist` есть `index.html`
- [x] В `dist/assets` есть JS и CSS файлы
- [x] В `dist/assets` есть папки с изображениями

### Проверьте размер:

```bash
du -sh dist/
ls -lh dist/assets/
```

**Нормальный размер:** 2-5 MB

## 4. Тестирование production build

### Запустите preview:

```bash
npm run preview
```

### Откройте http://localhost:4173 и проверьте:

- [ ] Главная страница загружается
- [ ] Все страницы работают
- [ ] Навигация работает
- [ ] Изображения отображаются
- [ ] Формы работают
- [ ] Нет ошибок в консоли

## 5. Подготовка Beget

### Регистрация и настройка:

- [ ] Зарегистрирован на https://beget.com
- [ ] Домен добавлен или создан поддомен
- [ ] Получены FTP/SSH данные для доступа
- [ ] Установлен FTP клиент (FileZilla, Cyberduck)

### Данные для подключения:

```
Хост: ваш-домен.beget.tech
Порт: 21 (FTP) или 22 (SSH)
Логин: ваш-логин
Пароль: ваш-пароль
```

## 6. Проверка Firebase

### Firebase Console:

- [ ] Открыт https://console.firebase.google.com
- [ ] Проект `repair-51654` существует
- [ ] Firestore Database создана
- [ ] Правила Firestore обновлены (см. `QUICK_SECURITY_FIX.md`)

### Правила Firestore:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /contacts/{contactId} {
      allow create: if isValidContact();
      allow read, update, delete: if false;
    }

    function isValidContact() {
      let data = request.resource.data;
      return
        data.keys().hasAll(['name', 'phone', 'createdAt', 'status']) &&
        data.name is string &&
        data.phone is string &&
        data.status is string &&
        data.name.size() > 0 && data.name.size() <= 100 &&
        data.phone.size() > 0 && data.phone.size() <= 20 &&
        data.status == 'new' &&
        data.createdAt is timestamp;
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
```

## 7. Проверка Telegram бота

### Telegram:

- [ ] Бот @novoselova_grammy_bot существует
- [ ] Вы подписаны на бота (нажали Start)
- [ ] Bot Token правильный
- [ ] Chat ID правильный

### Проверка через @userinfobot:

1. Найдите @userinfobot в Telegram
2. Отправьте ему любое сообщение
3. Проверьте что ваш ID = `1704115979`

## 8. Финальная проверка кода

### Запустите проверки:

```bash
# Проверка синтаксиса
npm run build

# Проверка зависимостей
npm audit

# Проверка размера
du -sh dist/
```

### Проверьте файлы:

- [ ] `dist/index.html` существует
- [ ] `dist/assets/*.js` существуют
- [ ] `dist/assets/*.css` существуют
- [ ] `dist/assets/client/*.png` существуют
- [ ] `dist/assets/header/*.svg` существуют
- [ ] `dist/assets/products/*.jpg` существуют

## 9. Документация готова

### Файлы документации:

- [x] `BEGET_DEPLOY_GUIDE.md` - полная инструкция
- [x] `.htaccess.example` - конфигурация Apache
- [x] `deploy.sh` - скрипт деплоя
- [x] `PRE_DEPLOY_CHECKLIST.md` - этот чеклист
- [x] `QUICK_SECURITY_FIX.md` - правила Firebase
- [x] `TELEGRAM_BOT_SETUP.md` - настройка бота

## 10. Готовность к деплою

### Все готово если:

- [ ] Все пункты выше отмечены ✅
- [ ] `npm run build` работает без ошибок
- [ ] `npm run preview` показывает рабочий сайт
- [ ] `.env.production` создан с правильными значениями
- [ ] Beget аккаунт настроен
- [ ] Firebase правила обновлены
- [ ] Telegram бот проверен

---

## 🚀 Следующий шаг

Если все пункты отмечены, переходите к деплою:

**Смотрите файл:** `BEGET_DEPLOY_GUIDE.md`

**Или запустите:**

```bash
./deploy.sh
```

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи сборки
2. Проверьте консоль браузера (F12)
3. Проверьте документацию в `BEGET_DEPLOY_GUIDE.md`
4. Обратитесь в поддержку Beget: support@beget.com

---

## ✅ Готово!

Проект готов к деплою на Beget! 🎉
